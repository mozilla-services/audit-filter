This is an example npm package with `audit-filter` configured and vulnerable dependencies.

The relevant files are:

* `.nsprc`
* `package.json`

See [the audit-filter README](../README.md) for additional details.
